# Bootstrap Run analysis.md

**Timestamp:** 20260526T141000Z

Historical evidence from init run.

## Repo snapshot

Template iFrame widget (webpack). No prior `.governance/`. README is template documentation, not product brief.

## Preflight sequence

1. git/gh/auth — pass
2. repo view — pass
3. project list — fail (read:project)
4. issue list — fail (issues disabled)
5. branch protection API — 404 unprotected main

## Canonical board selection

Not reached — preflight blocked board mutation per contract.

## Mirror targets

None — no provider rules directory.
